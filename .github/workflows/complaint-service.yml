name: Complaint Service CI/CD

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'backend/services/complaint-service/**'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'backend/services/complaint-service/**'

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: iog_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      
      redis:
        image: redis:7
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
      
      mongodb:
        image: mongo:6
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 27017:27017
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: backend/services/complaint-service/package-lock.json
      
      - name: Install dependencies
        working-directory: backend/services/complaint-service
        run: npm ci
      
      - name: Run linter
        working-directory: backend/services/complaint-service
        run: npm run lint || true
      
      - name: Build TypeScript
        working-directory: backend/services/complaint-service
        run: npm run build
      
      - name: Run tests
        working-directory: backend/services/complaint-service
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/iog_test
          MONGODB_URL: mongodb://localhost:27017
          REDIS_URL: redis://localhost:6379
        run: npm test || echo "No tests configured"

  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        working-directory: backend/services/complaint-service
        run: npm ci
      
      - name: Build
        working-directory: backend/services/complaint-service
        run: npm run build
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: complaint-service-build
          path: backend/services/complaint-service/dist
